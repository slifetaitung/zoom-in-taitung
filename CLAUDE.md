# Zoom in Taitung 線上翻頁書

把《Zoom in Taitung》各期 PDF 轉成網頁翻頁書，嵌進既有的 Google Sites
（臺東造句・造聚）。目前以 vol.2 作為第一個實作對象。

## 硬性需求

- **必須是翻頁，不能是垂直捲動。** 這是整個專案存在的理由，任何改動都不能破壞它。
- 產出必須能用 `<iframe>` 嵌進 Google Sites（純靜態、HTTPS）。
- 手機直向必須是單頁。手機橫向：**大螢幕機種（寬 ≥896px）顯示跨頁**，小螢幕仍是單頁。
  （原訂「橫向也要維持單頁」，2026-08-28 依實測數據調整，理由見下方陷阱那條。）
- 不要引入 build step。純靜態檔案，`python3 -m http.server` 就能跑。

## 技術選型與其代價

用 **StPageFlip**（npm `page-flip`）。選它是因為可自行掌控翻頁行為，不會被
服務商改版影響。但要知道：

- 這個函式庫實質上已停止維護（npm 最新 2.0.7，約五年前）。
- **不要用 CDN 連結**，把 `page-flip.browser.js` 下載進 repo 自行託管。
- 壞掉沒有官方支援，只能自己修。程式碼不大、MIT 授權，可行。

## 已知陷阱（踩過才知道，優先遵守）

**判斷跨頁的是 `minWidth`，不是 `width`。**
StPageFlip 的實際判斷式在 `Render.ts`：`容器寬度 < minWidth × 2 → 單頁`。
設定裡的 `width` 只在 `size:'fixed'` 時有用，`stretch` 模式看的是 `minWidth`。

**目前 `MIN_PAGE_W = 440`（門檻 880px），不要低於這個值。**
2026-08-28 從 500 調下來，讓大螢幕手機橫向也能看跨頁。
原本怕橫向跨頁後單頁只剩 400px 出頭，但書本尺寸是由**高度**回推的
（要整本塞進固定高度的 iframe），單頁模式本來也只有 506px，
改跨頁後 458px，只縮 9%。再往下調，iPhone SE 橫向會變成每頁 325px，
內文會小到讀不了。

**StPageFlip 會把 `minWidth`/`minHeight` 寫成 inline style 貼在書本元素上。**
容器再小書也不縮，超出的部分被 `overflow:hidden` 裁掉 —— 書是置中的，
所以會變成「左半邊整個消失」。`book.css` 裡用
`#book{min-width:0!important;min-height:0!important;}` 蓋掉，那行不能刪。

**總頁數必須是偶數。**
`showCover: true` 讓封面單獨一頁。要讓封底也單獨一頁，總頁數得是偶數。
vol.2 若為奇數頁，在最後補一張空白頁。

**PDF 必須是螢幕版，不是印刷版。**
印刷版帶 3mm 出血和裁切標記，轉圖後每頁邊緣會有黑線。也要確認是 RGB
不是 CMYK，否則顏色會偏。

**匯出必須是單頁（Pages）不是跨頁（Spreads）。**
拿到跨頁檔就得再切一次，切點容易壓到跨頁大圖的接縫。

**sRGB 描述檔不能省。**
壓縮時用了 `-strip` 清中繼資料後，要用 `-profile` 把 sRGB 加回去，
否則 Safari 會用螢幕原生色域顯示，照片偏艷。

**檔名排序要用 `sort -V`。**
一般的 `sort` 會把 `_10` 排在 `_2` 前面。

**Google Sites 的 iframe 高度是寫死的**，不會隨內容伸縮。**目前用 760px**。
書本尺寸由高度回推，所以高度不夠時欄寬再大也湊不到跨頁門檻
（高 700 時總寬永遠卡在 924，桌機也會退回單頁）。

已知未解：手機橫向可見高度只有 375–440px，但 iframe 是 760px，
所以橫向要捲動約 200px 才看得完整頁。2026-08-28 決定先不處理。

## 圖片規格

| 用途 | 長邊 | 格式 | 品質 |
|---|---|---|---|
| 翻頁書顯示 | 2000px | WebP 優先，JPEG 備援 | q80–82 |
| 雙擊放大 | 3000px | JPEG | q82 |

單頁 300–500KB，全書控制在 60MB 以內。

## 手機設計原則

手機上 A4 內文換算只剩約 6px，物理上讀不了。所以手機版的目標是「瀏覽 →
雙擊放大細看」兩層路徑，不是在手機上讀完整本。

- 翻頁動畫 450ms（比桌機快），`drawShadow: false` 省效能
- `swipeDistance: 20`（預設 30 在小螢幕偏遲鈍）
- 觸控目標至少 44×44px，底部用 `env(safe-area-inset-bottom)`
- 全螢幕按鈕和跳頁輸入框在手機上隱藏

## 範圍控制

**要做的：** 翻頁瀏覽、雙擊放大、鍵盤左右鍵。

（原本有「下載完整 PDF」，2026-08-28 依版權考量移除。）

**不做的（除非有人真的反映）：** 全文搜尋、縮圖目錄、閱讀統計、密碼保護、
RTL 右翻模式。這些單獨看都不難，加起來等於把商業服務重寫一遍。

## 驗證方式

改完一定要在**發布後的實際 Google Sites 網址**上用真機測，不要只看
Claude Code 的本地預覽或瀏覽器的裝置模擬器 —— 觸控衝突和 safe area
在模擬器上不會重現。

必測五項：
1. iPhone 直向是單頁
2. iPhone 橫向：大螢幕機種（15PM 以上）是跨頁且兩頁都完整；小螢幕仍是單頁
3. 雙擊能放大
4. 手指在書上滑動時，外層 Google Sites 仍能捲動
5. 底部按鈕沒有被系統列蓋住

## 交接

這是實習期間的專案，之後會交給非工程背景的同事。因此：

- GitHub repo 要建在**單位的帳號**下，不是個人帳號
- 每個腳本都要有中文註解，README 要寫成非工程師能照做的步驟
- 新增一期的流程要能用同一支腳本跑完，不需要改程式碼

## 版權

刊物版權屬微慢城鄉生活顧問有限公司，實體書有定價。全本公開上線前要先
確認授權範圍（全本 vs 試閱）。在確認之前，測試頁一律設為「從導覽中隱藏」。
